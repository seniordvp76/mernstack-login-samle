import React, { Component } from 'react';

class Loading extends Component {
    // eslint-disable-next-line
    constructor(props) {
        super(props);
    }
    render() {
        return (
            <div class="card-header border-bottom">Loading...</div>
        );
    }
};

export default Loading;